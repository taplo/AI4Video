---
phase: 03-architecture-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/settings.py
  - app/apps.py
  - app/utils/Config.py
  - .env
  - .gitignore
autonomous: false
requirements:
  - D-01
  - D-03
  - D-09
  - D-10
  - D-11
  - D-12
user_setup: []
must_haves:
  truths:
    - "SQLite connections use WAL journal mode (not DELETE)"
    - "SECRET_KEY is read from .env file via environment variable"
    - "Sensitive values (safe key, media secret, SIP password) are in .env, not config.json"
    - "Config class is thread-safe with RLock for hot reload"
    - "Django PRAGMA settings use busy_timeout, cache_size, temp_store, WAL, foreign_keys"
  artifacts:
    - path: ".env"
      provides: "Sensitive configuration values"
      contains: "DJANGO_SECRET_KEY"
    - path: "framework/settings.py"
      provides: "SECRET_KEY from env, load_dotenv integration"
      contains: "load_dotenv"
    - path: "app/apps.py"
      provides: "WAL mode PRAGMAs via connection_created signal"
      contains: "journal_mode=WAL"
    - path: "app/utils/Config.py"
      provides: "Thread-safe Config with RLock"
      contains: "threading.RLock"
    - path: ".gitignore"
      provides: ".env excluded from version control"
      contains: ".env"
  key_links:
    - from: "framework/settings.py"
      to: ".env"
      via: "load_dotenv()"
      pattern: "load_dotenv"
    - from: "app/apps.py"
      to: "SQLite connection"
      via: "connection_created signal"
      pattern: "connection_created.connect"
    - from: "app/utils/Config.py"
      to: "threading.RLock"
      via: "_lock attribute"
      pattern: "self._lock = threading.RLock"
---

<objective>
Infrastructure foundation: SQLite WAL mode, .env sensitive config, Config thread safety.

Purpose: Enable WAL mode BEFORE removing g_dbLock (dependency chain). Set up .env for secrets. Make Config thread-safe for hot reload.
Output: WAL-enabled SQLite, .env file with secrets, thread-safe Config class.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-architecture-upgrade/03-CONTEXT.md
@.planning/phases/03-architecture-upgrade/03-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@framework/settings.py
@app/apps.py
@app/utils/Config.py
@config.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create .env file</name>
  <files>.env, .gitignore, requirements.txt (new)</files>
  <read_first>
    - config.json (to extract current sensitive values for migration)
    - framework/settings.py (to understand current SECRET_KEY usage)
    - .gitignore (to add .env entry)
  </read_first>
  <action>
**Step 1: Verify package legitimacy** (per D-07, D-09)
Run `pip index versions django-fernet-fields` and `pip index versions python-dotenv` to confirm packages exist on PyPI with download history. Both are 10+ year old packages with high download counts.

**Step 2: Install packages** (per D-07, D-09)
Run: `uv pip install django-fernet-fields python-dotenv`

**Step 3: Create .env file at project root** (per D-11, D-12)
Create `.env` with these sensitive values extracted from config.json and framework/settings.py:
```
DJANGO_SECRET_KEY=ai4video-dev-insecure-key-change-in-production
AI4VIDEO_SAFE_KEY=ai4video_safe_key_2026
AI4VIDEO_MEDIA_SECRET=aqxY9ps21fyhyKNRyYpGvJCTp1JBeGOM
AI4VIDEO_SIP_PASSWORD=123456
```
Use the actual values currently in config.json (`safe`, `mediaSecret`, `sipServer.sipServerPass`) and the SECRET_KEY from settings.py.

**Step 4: Add .env to .gitignore**
Append `.env` to the end of `.gitignore`.

**Step 5: Create requirements.txt** (optional but good practice)
Create `requirements.txt` listing the new dependencies added in this phase.
  </action>
  <verify>
    <automated>
      # Verify .env exists and contains required keys
      Test-Path ".env" -eq $true
      # Verify .gitignore contains .env
      Select-String -Path ".gitignore" -Pattern "^\.env$" -Quiet
      # Verify packages installed
      uv pip show django-fernet-fields 2>$null | Select-String "django-fernet-fields"
      uv pip show python-dotenv 2>$null | Select-String "python-dotenv"
    </automated>
  </verify>
  <acceptance_criteria>
    - .env file exists at project root with DJANGO_SECRET_KEY, AI4VIDEO_SAFE_KEY, AI4VIDEO_MEDIA_SECRET, AI4VIDEO_SIP_PASSWORD
    - .gitignore contains ".env" on its own line
    - `uv pip show django-fernet-fields` returns non-empty output
    - `uv pip show python-dotenv` returns non-empty output
    - .env values match the current config.json values: safe="ai4video_safe_key_2026", mediaSecret="aqxY9ps21fyhyKNRyYpGvJCTp1JBeGOM", sipServerPass="123456"
  </acceptance_criteria>
  <done>packages installed, .env created with sensitive values, .gitignore updated</done>
</task>

<task type="auto">
  <name>Task 2: Enable SQLite WAL mode via connection_created signal</name>
  <files>app/apps.py, framework/settings.py</files>
  <read_first>
    - app/apps.py (current PRAGMA setup at lines 22-33, signal receiver pattern)
    - framework/settings.py (current DATABASES config at lines 87-96)
  </read_first>
  <action>
**Step 1: Update framework/settings.py** (per D-01, D-09)
Add `from dotenv import load_dotenv` at the very top of settings.py (line 1, before all other imports except `time`). Then call `load_dotenv(BASE_DIR / '.env')` immediately after. This ensures SECRET_KEY is loaded from .env before django-fernet-fields tries to use it (Pitfall 2 from RESEARCH.md).

The current line 27 already reads: `SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'ai4video-dev-insecure-key-change-in-production')`. This will now work correctly because load_dotenv populates os.environ.

**Step 2: Update app/apps.py SQLite PRAGMAs** (per D-01)
Replace the existing `_setup_sqlite_pragma` function (lines 24-31) with WAL-mode PRAGMAs. The current setup uses `journal_mode=DELETE` — change to WAL for concurrent read/write support.

Replace the PRAGMA block inside `_setup_sqlite_pragma` with:
```python
cur.execute('PRAGMA journal_mode=WAL;')
cur.execute('PRAGMA synchronous=NORMAL;')
cur.execute('PRAGMA foreign_keys=ON;')
cur.execute('PRAGMA busy_timeout=5000;')
cur.execute('PRAGMA temp_store=MEMORY;')
cur.execute('PRAGMA mmap_size=134217728;')  # 128MB mmap
cur.execute('PRAGMA cache_size=-2000;')  # 2MB page cache
```

Note: The existing function already has the correct structure (connection_created signal receiver). Just change the PRAGMA values. Do NOT change the signal registration pattern — it's already correct for Django 5.0.4.
  </action>
  <verify>
    <automated>
      # Verify load_dotenv in settings.py
      Select-String -Path "framework/settings.py" -Pattern "from dotenv import load_dotenv" -Quiet
      # Verify WAL PRAGMA in apps.py
      Select-String -Path "app/apps.py" -Pattern "journal_mode=WAL" -Quiet
      # Verify load_dotenv call in settings.py
      Select-String -Path "framework/settings.py" -Pattern "load_dotenv\(BASE_DIR" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - framework/settings.py line 1: `from dotenv import load_dotenv`
    - framework/settings.py has `load_dotenv(BASE_DIR / '.env')` before SECRET_KEY assignment
    - framework/settings.py SECRET_KEY reads from os.environ (existing pattern preserved)
    - app/apps.py `_setup_sqlite_pragma` function contains `PRAGMA journal_mode=WAL;`
    - app/apps.py contains `PRAGMA synchronous=NORMAL;`
    - app/apps.py contains `PRAGMA foreign_keys=ON;`
    - app/apps.py contains `PRAGMA busy_timeout=5000;`
    - app/apps.py contains `PRAGMA temp_store=MEMORY;`
    - app/apps.py `connection_created.connect(_setup_sqlite_pragma)` still present
  </acceptance_criteria>
  <done>SQLite WAL mode enabled via connection_created signal, SECRET_KEY loaded from .env before model imports</done>
</task>

<task type="auto">
  <name>Task 3: Add threading.RLock to Config class for thread-safe hot reload</name>
  <files>app/utils/Config.py</files>
  <read_first>
    - app/utils/Config.py (full file, understand _apply and save_from_web patterns)
  </read_first>
  <action>
**Step 1: Add threading import** (per D-10)
Add `import threading` at the top of Config.py (after existing `import json` and `import os`).

**Step 2: Add RLock to Config.__init__** (per D-10)
In the `__init__` method, after `self.__config_data = config_data`, add:
```python
self._lock = threading.RLock()
```

**Step 3: Wrap _apply with lock** (per D-10)
Wrap the body of `_apply` method with `with self._lock:` to make config reads/writes thread-safe. The method currently mutates self attributes directly (self.safe, self.externalHost, etc.) — these must be protected.

**Step 4: Wrap save_from_web with lock** (per D-10)
Wrap the file write + _apply call in `save_from_web` with `with self._lock:` to ensure atomic read-modify-write of config.json.

**Step 5: Wrap reload-related access with lock** (per D-10)
The `to_dict` method should also use the lock when reading config_data to ensure consistent snapshots.
  </action>
  <verify>
    <automated>
      # Verify threading import
      Select-String -Path "app/utils/Config.py" -Pattern "import threading" -Quiet
      # Verify RLock in __init__
      Select-String -Path "app/utils/Config.py" -Pattern "self._lock = threading.RLock\(\)" -Quiet
      # Verify _apply uses lock
      Select-String -Path "app/utils/Config.py" -Pattern "with self._lock:" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - Config.py imports threading
    - Config.__init__ creates self._lock = threading.RLock()
    - Config._apply method body is wrapped with `with self._lock:`
    - Config.save_from_web uses `with self._lock:` for the write+_apply section
    - Config.to_dict reads config_data under `with self._lock:`
    - No existing Config functionality is broken (same public API)
  </acceptance_criteria>
  <done>Config class is thread-safe with RLock protecting all read/write operations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → settings.py | Secrets loaded from filesystem into Django settings |
| SQLite connection → PRAGMAs | PRAGMA settings applied per-connection via signal |
| Config hot reload → threads | Config mutations protected by RLock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | .env file | mitigate | .env in .gitignore; never commit to version control |
| T-03-02 | Tampering | SQLite PRAGMAs | accept | PRAGMAs applied per-connection; no external input can override |
| T-03-03 | Elevation of Privilege | Config hot reload | mitigate | RLock prevents race conditions during concurrent config updates |
</threat_model>

<verification>
- WAL mode active: Django app starts without "database is locked" errors under concurrent requests
- .env loaded: SECRET_KEY matches the value in .env file
- Config thread-safe: Config class has RLock on _apply, save_from_web, to_dict
</verification>

<success_criteria>
- SQLite journal_mode returns 'wal' after connection
- .env file exists with 4 sensitive values
- .gitignore excludes .env
- Config class uses threading.RLock for all mutations
- Django app starts without import errors
</success_criteria>

<output>
Create `.planning/phases/03-architecture-upgrade/03-01-SUMMARY.md` when done
</output>
