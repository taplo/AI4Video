---
phase: 03-architecture-upgrade
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/models.py
  - app/utils/Database.py
autonomous: true
requirements:
  - D-04
  - D-05
  - D-07
  - D-08
user_setup: []
must_haves:
  truths:
    - "g_dbLock is completely removed from the codebase"
    - "All 8 model classes inherit from BaseModel"
    - "BaseModel provides save/delete methods without lock wrappers"
    - "ThreadSafetyManager is removed (no longer needed with WAL)"
    - "StreamModel.pull_stream_password is encrypted with django-fernet-fields"
    - "LLMModel.api_key is encrypted with django-fernet-fields"
    - "Database.py no longer uses g_dbLock"
  artifacts:
    - path: "app/models.py"
      provides: "BaseModel mixin + refactored model classes + EncryptedCharField"
      contains: "class BaseModel"
    - path: "app/utils/Database.py"
      provides: "Simplified Database class without g_dbLock"
      contains: "class Database"
  key_links:
    - from: "app/models.py"
      to: "django-fernet-fields"
      via: "EncryptedCharField import"
      pattern: "from fernet_fields import EncryptedCharField"
    - from: "app/models.py"
      to: "BaseModel"
      via: "class inheritance"
      pattern: "class.*BaseModel\)"
    - from: "app/utils/Database.py"
      to: "django.db.connection"
      via: "direct cursor access"
      pattern: "connection\.cursor\(\)"
---

<objective>
Model layer refactoring: BaseModel mixin, remove g_dbLock, add encrypted fields.

Purpose: Eliminate duplicated save/delete patterns across 8 models, remove global lock (WAL handles concurrency now), encrypt sensitive fields.
Output: BaseModel class, refactored models, simplified Database.py, EncryptedCharField on sensitive fields.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-architecture-upgrade/03-CONTEXT.md
@.planning/phases/03-architecture-upgrade/03-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@app/models.py
@app/utils/Database.py
@.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseModel mixin, refactor all models, add encrypted fields</name>
  <files>app/models.py</files>
  <read_first>
    - app/models.py (full 427-line file, understand all 8 model classes and their save/delete patterns)
  </read_first>
  <action>
**Step 1: Add imports** (per D-07, D-08)
At the top of models.py, add:
```python
from fernet_fields import EncryptedCharField
```
Remove the existing import `from app.utils.Database import g_dbLock` (line 2).

**Step 2: Create BaseModel** (per D-05, D-08)
Add BaseModel class BEFORE ThreadSafetyManager (replace lines 6-10):
```python
class BaseModel(models.Model):
    """Base model — eliminates duplicated save/delete across all models.
    WAL mode handles concurrency; no lock needed."""

    class Meta:
        abstract = True

    def save(self, force_insert=False, force_update=False, using=None, update_fields=None):
        super().save(force_insert, force_update, using, update_fields)

    def delete(self, using=None, keep_parents=False):
        super().delete(using, keep_parents)
```

**Step 3: Remove ThreadSafetyManager** (per D-04)
Delete the ThreadSafetyManager class entirely (lines 6-10). It's no longer needed since WAL handles concurrency.

**Step 4: Refactor StreamModel** (per D-04, D-05, D-07, D-08)
- Change `class StreamModel(models.Model)` → `class StreamModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Change `pull_stream_password = models.CharField(max_length=50, ...)` → `pull_stream_password = EncryptedCharField(max_length=50, ...)`
- Remove the `delete` method (lines 68-71)
- Remove the `save` method (lines 73-76)

**Step 5: Refactor AlgorithmModel** (per D-04, D-05, D-08)
- Change `class AlgorithmModel(models.Model)` → `class AlgorithmModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Remove the `delete` method (lines 160-163)
- Remove the `save` method (lines 165-168)

**Step 6: Refactor BizAlgorithmModel** (per D-04, D-05, D-08)
- Change `class BizAlgorithmModel(models.Model)` → `class BizAlgorithmModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Remove the `delete` method (lines 239-242)
- Remove the `save` method (lines 244-247)

**Step 7: Refactor ZoneModel** (per D-04, D-05, D-08)
- Change `class ZoneModel(models.Model)` → `class ZoneModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Remove the `delete` method (lines 283-286)
- Remove the `save` method (lines 288-291)

**Step 8: Refactor AlarmModel** (per D-04, D-05, D-08)
- Change `class AlarmModel(models.Model)` → `class AlarmModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Remove the `delete` method (lines 321-324)
- Remove the `save` method (lines 326-329)

**Step 9: Refactor RecordingModel** (per D-04, D-05, D-08)
- Change `class RecordingModel(models.Model)` → `class RecordingModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- RecordingModel doesn't have custom save/delete — just update inheritance

**Step 10: Refactor LLMModel** (per D-04, D-05, D-07, D-08)
- Change `class LLMModel(models.Model)` → `class LLMModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Change `api_key = models.CharField(max_length=200, default='', ...)` → `api_key = EncryptedCharField(max_length=200, default='', ...)`
- Remove the `delete` method (lines 382-385)
- Remove the `save` method (lines 387-390)

**Step 11: Refactor LogModel** (per D-04, D-05, D-08)
- Change `class LogModel(models.Model)` → `class LogModel(BaseModel)`
- Remove `objects = ThreadSafetyManager()`
- Remove the `delete` method (lines 414-417)
- Remove the `save` method (lines 419-422)

**Step 12: Remove json import** (cleanup)
Remove `import json` from line 3 if no longer needed after removing the raw string labels handling. Check if json is used elsewhere in the file — if not, remove it.
  </action>
  <verify>
    <automated>
      # Verify BaseModel exists
      Select-String -Path "app/models.py" -Pattern "class BaseModel\(models.Model\)" -Quiet
      # Verify g_dbLock is completely removed
      Select-String -Path "app/models.py" -Pattern "g_dbLock" -Quiet
      # Should return False (no matches)
      # Verify ThreadSafetyManager is removed
      Select-String -Path "app/models.py" -Pattern "ThreadSafetyManager" -Quiet
      # Should return False (no matches)
      # Verify EncryptedCharField imported
      Select-String -Path "app/models.py" -Pattern "from fernet_fields import EncryptedCharField" -Quiet
      # Verify all models inherit from BaseModel
      Select-String -Path "app/models.py" -Pattern "class \w+\(BaseModel\)" -Quiet
      # Verify pull_stream_password is EncryptedCharField
      Select-String -Path "app/models.py" -Pattern "pull_stream_password = EncryptedCharField" -Quiet
      # Verify api_key is EncryptedCharField
      Select-String -Path "app/models.py" -Pattern "api_key = EncryptedCharField" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - models.py imports `from fernet_fields import EncryptedCharField`
    - models.py does NOT import `from app.utils.Database import g_dbLock`
    - models.py contains `class BaseModel(models.Model)` with `class Meta: abstract = True`
    - models.py does NOT contain `class ThreadSafetyManager`
    - StreamModel(BaseModel), AlgorithmModel(BaseModel), BizAlgorithmModel(BaseModel), ZoneModel(BaseModel), AlarmModel(BaseModel), RecordingModel(BaseModel), LLMModel(BaseModel), LogModel(BaseModel) all present
    - No model class has custom save() or delete() methods (all removed)
    - `pull_stream_password = EncryptedCharField(max_length=50, ...)` present
    - `api_key = EncryptedCharField(max_length=200, ...)` present
    - No `objects = ThreadSafetyManager()` in any model
    - All existing model fields preserved (no fields removed or renamed)
    - db_table Meta options preserved for all models
  </acceptance_criteria>
  <done>All 8 models inherit from BaseModel, g_dbLock removed, EncryptedCharField on pull_stream_password and api_key</done>
</task>

<task type="auto">
  <name>Task 2: Simplify Database.py — remove g_dbLock</name>
  <files>app/utils/Database.py</files>
  <read_first>
    - app/utils/Database.py (full 37-line file, understand g_dbLock usage)
  </read_first>
  <action>
**Step 1: Remove g_dbLock** (per D-04)
Delete the `g_dbLock = threading.Lock()` line (line 3) and the `import threading` (line 1).

**Step 2: Remove lock usage from select()** (per D-04)
In `select()` method, remove the `with g_dbLock:` context manager (lines 11-12). The method should directly execute the SQL without locking. WAL mode handles concurrency.

**Step 3: Remove lock usage from execute()** (per D-04)
In `execute()` method, remove the `with g_dbLock:` context manager (lines 29-30). Same reason — WAL handles concurrency.

**Step 4: Keep the rest unchanged**
The Database class interface (select, execute methods) stays the same. Only the lock is removed.
  </action>
  <verify>
    <automated>
      # Verify g_dbLock is removed
      Select-String -Path "app/utils/Database.py" -Pattern "g_dbLock" -Quiet
      # Should return False
      # Verify threading import is removed
      Select-String -Path "app/utils/Database.py" -Pattern "import threading" -Quiet
      # Should return False
      # Verify Database class still exists
      Select-String -Path "app/utils/Database.py" -Pattern "class Database" -Quiet
      # Verify select method exists
      Select-String -Path "app/utils/Database.py" -Pattern "def select\(" -Quiet
      # Verify execute method exists
      Select-String -Path "app/utils/Database.py" -Pattern "def execute\(" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - Database.py does NOT contain `g_dbLock`
    - Database.py does NOT import `threading`
    - Database.py still has `class Database` with `select()` and `execute()` methods
    - Database.py uses `connection.cursor()` directly (no lock context)
    - The `from django.db import connection` import is preserved
  </acceptance_criteria>
  <done>Database.py simplified — g_dbLock removed, WAL handles concurrency</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Encrypted fields → database | Sensitive values encrypted before DB write |
| BaseModel → model lifecycle | save/delete overridden for all models |
| Database.py → SQLite | Direct cursor access without application-level lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Encrypted fields | mitigate | django-fernet-fields uses Fernet symmetric encryption with SECRET_KEY |
| T-03-08 | Tampering | g_dbLock removal | mitigate | WAL mode provides database-level concurrency; no application lock needed |
| T-03-09 | Denial of Secret | SECRET_KEY dependency | accept | If SECRET_KEY changes, encrypted fields become unreadable — documented constraint |
</threat_model>

<verification>
- g_dbLock grep returns 0 matches across entire app/ directory
- All 8 models inherit from BaseModel
- EncryptedCharField used for pull_stream_password and api_key
- Database.py has no threading imports or lock usage
</verification>

<success_criteria>
- `grep -r "g_dbLock" app/` returns 0 matches
- `grep -c "class.*BaseModel" app/models.py` returns at least 1
- `grep "EncryptedCharField" app/models.py` returns 2 matches (pull_stream_password, api_key)
- All model classes have no custom save/delete methods
- Database.py has no threading or lock imports
</success_criteria>

<output>
Create `.planning/phases/03-architecture-upgrade/03-03-SUMMARY.md` when done
</output>
