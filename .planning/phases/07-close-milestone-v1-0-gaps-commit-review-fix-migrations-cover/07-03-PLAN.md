---
phase: 07
plan: 07-03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - .github/workflows/test.yml
  - .planning/v1.0-VERIFICATION.md
  - .planning/phases/01-rename-cleanup/01-01-SUMMARY.md
  - .planning/phases/02-onnx-fix/02-01-SUMMARY.md
  - .planning/phases/03-architecture-upgrade/03-01-SUMMARY.md
  - .planning/phases/04-engineering-hardening/04-01-SUMMARY.md
  - .planning/phases/05-test-infrastructure/05-01-SUMMARY.md
  - .planning/phases/06-other-upgrades/06-01-SUMMARY.md
  - .planning/phases/07-close-milestone-v1-0-gaps-commit-review-fix-migrations-cover/07-01-SUMMARY.md
autonomous: true
requirements:
  - phase05-D-08
  - phase05-D-12
  - phase04-REVIEW
  - phase07-D-03
  - phase07-D-14
---

# Plan 07-03: CI Gate + Docs + Cleanup

## Objective

Add makemigrations --check CI gate, produce milestone-level verification documentation, backfill SUMMARY.md frontmatter, write the phase summary, and clean up the stale review-fix branch/worktree.

## must_haves

- .github/workflows/test.yml has makemigrations --check step with DJANGO_SECRET_KEY + DEBUG env
- v1.0-VERIFICATION.md exists with status: passed
- 01..06 SUMMARY.md files have requirements-completed frontmatter (D-14)
- 07-SUMMARY.md written (D-12: coverage journey 17%→60%)
- Stale branch gsd-reviewfix/06-17660 deleted (D-03)
- Stale worktree removed (D-03)

## truths

- CI gate is D-08 (CI enforces migration drift check symmetrically with local)
- v1.0-VERIFICATION.md aggregates evidence from all 7 phases
- D-14 requires requirements-completed backfill in all prior SUMMARY.md files
- D-12 requires coverage journey documentation (baseline 17% → 60% with per-module deltas)
- D-03 requires post-verify cleanup: worktree remove + branch delete + marker file delete
- Cleanup MUST happen only after all verification passes

## threats

- T-07-05: Worktree removal fails if worktree is dirty — must force
- T-07-06: Branch deletion fails if worktree still linked — must remove worktree first
- T-07-07: makemigrations --check fails if DJANGO_SECRET_KEY not set in CI env

## <threat_model>

- No security-relevant code changes in this plan
- CI gate prevents migration drift (security-relevant for AuditLog table)
- v1.0-VERIFICATION.md is a trust anchor for milestone sign-off

</threat_model>

<task id="07-03-01" type="execute" priority="high">
<summary>Add makemigrations --check step to CI</summary>

<read_first>
- .github/workflows/test.yml (current CI config — find test step, see env block with DJANGO_SECRET_KEY)
</read_first>

<action>
Edit .github/workflows/test.yml to add a new step after "Install dependencies" and before the test step:
```yaml
- name: Check for missing migrations
  env:
    DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY || 'ci-test-secret-key' }}
    DEBUG: "true"
  run: uv run python manage.py makemigrations --check --dry-run app
```
This mirrors the local `makemigrations --check` gate (phase06-D-12) in CI.
</action>

<acceptance_criteria>
- `.github/workflows/test.yml` contains `makemigrations --check --dry-run app`
- Step has `DJANGO_SECRET_KEY` and `DEBUG` env vars
- Step is positioned after "Install dependencies" and before the test step
- YAML is valid (no syntax errors)
</acceptance_criteria>
</task>

<task id="07-03-02" type="execute" priority="high">
<summary>Write v1.0-VERIFICATION.md</summary>

<read_first>
- .planning/v1.0-MILESTONE-AUDIT.md (gap source — all findings)
- .planning/phases/07-close-milestone-v1-0-gaps-commit-review-fix-migrations-cover/07-RESEARCH.md (Validation Architecture section)
- .planning/STATE.md (project state)
</read_first>

<action>
Create .planning/v1.0-VERIFICATION.md at the project .planning/ root (NOT inside the phase directory) with:
- Frontmatter: milestone: v1.0, status: passed, date: 2026-08-11
- Section: Evidence per phase (01 through 07) — one block per phase with what was verified
- Section: Coverage journey — baseline 17% → 60% table
- Section: Security review — CR-01/CR-02 closed, AuditLog in production
- Section: CI gates — test.yml + makemigrations + verify.ps1
- Section: Sign-off — ready for milestone closure
</action>

<acceptance_criteria>
- `.planning/v1.0-VERIFICATION.md` exists
- File contains `status: passed` in frontmatter
- File contains coverage journey table (17% → 60%)
- File references all 7 phases by number
</acceptance_criteria>
</task>

<task id="07-03-03" type="execute" priority="medium">
<summary>Backfill requirements-completed in 01..06 SUMMARY.md</summary>

<read_first>
- .planning/phases/01-rename-cleanup/01-01-SUMMARY.md (existing frontmatter)
- .planning/phases/02-onnx-fix/02-01-SUMMARY.md (existing frontmatter)
- .planning/phases/03-architecture-upgrade/03-01-SUMMARY.md (existing frontmatter)
- .planning/phases/04-engineering-hardening/04-01-SUMMARY.md (existing frontmatter)
- .planning/phases/05-test-infrastructure/05-01-SUMMARY.md (existing frontmatter)
- .planning/phases/06-other-upgrades/06-01-SUMMARY.md (existing frontmatter)
- .planning/ROADMAP.md (phase requirement IDs per phase)
</read_first>

<action>
For each of phases 01..06, add `requirements-completed:` field to the SUMMARY.md frontmatter with the REQ-IDs or D## codes that were addressed. This is D-14 backfill. Read each SUMMARY.md, identify what was done, map to requirement IDs from ROADMAP.md, add the field.
</action>

<acceptance_criteria>
- Each of 01-01-SUMMARY.md through 06-01-SUMMARY.md contains `requirements-completed:` in frontmatter
- Values are non-empty (at least one requirement ID per file)
- `grep -l "requirements-completed:" .planning/phases/0{1,2,3,4,5,6}-*/*-SUMMARY.md` returns 6 files
</acceptance_criteria>
</task>

<task id="07-03-04" type="execute" priority="high">
<summary>Write 07-SUMMARY.md — coverage journey</summary>

<read_first>
- .planning/phases/05-test-infrastructure/05-01-SUMMARY.md (format reference)
- .planning/phases/06-other-upgrades/06-01-SUMMARY.md (format reference)
- .planning/v1.0-MILESTONE-AUDIT.md (coverage data: 12,706 stmts / 2,159 covered / 17%)
- pytest.ini (addopts)
</read_first>

<action>
Create .planning/phases/07-close-milestone-v1-0-gaps-commit-review-fix-migrations-cover/07-01-SUMMARY.md with:
- Frontmatter: phase: 07, status: complete, requirements-completed: [phase06-REVIEW, phase05-D-05, phase05-D-08, phase05-D-12, phase04-REVIEW, phase07-D-01..D-15]
- Section: Coverage journey (D-12) — table showing per-module deltas:
  - Baseline: 12,706 stmts, 2,159 covered (17%)
  - Final: ≥7,624 covered (≥60%)
  - Per-module breakdown: AnalysisManager, pipeline, StreamView, ControlView, SmallModelView, InnerlView, NvrView, LLMView, IndexView, SystemView, GpuInfo, ZLMediaKitApi, OSSystem, MediaServerManager, GlobalUtils
  - Justification for low-coverage modules (GB28181/SIP protocol — hardware-dependent)
- Section: What was built — 3 plans, git-ops commits + tests + CI + docs
- Section: Gaps resolved — CR-01, CR-02, WR-01..09, coverage gate, CI gate
</action>

<acceptance_criteria>
- `07-01-SUMMARY.md` exists in phase directory
- File contains `coverage` section with baseline → target table
- File contains `requirements-completed` in frontmatter with at least 3 entries
</acceptance_criteria>
</task>

<task id="07-03-05" type="execute" priority="high">
<summary>Post-verify cleanup — worktree + branch + marker</summary>

<read_first>
- .review-fix-recovery-pending.json (marker file — exists from phase06)
- Git worktree list output (check for stale worktrees)
</read_first>

<action>
**WARNING: Only run this task AFTER all verification passes (full test suite green, git status clean).**

1. `git worktree remove --force <stale-worktree-path>` (the gsd-reviewfix/06-17660 worktree)
2. `git branch -D gsd-reviewfix/06-17660`
3. `Remove-Item .review-fix-recovery-pending.json` (D-03 marker file)
4. Commit: `git commit -am "chore: cleanup stale review-fix branch and marker (D-03)"`
</action>

<acceptance_criteria>
- `git worktree list` does NOT show gsd-reviewfix/06-17660
- `git branch | Select-String "gsd-reviewfix"` returns empty
- `.review-fix-recovery-pending.json` does NOT exist
- `git status` shows clean working tree
</acceptance_criteria>
</task>

<task id="07-03-06" type="execute" priority="high">
<summary>Verify Plan 07-03 — Full suite + CI gate + git clean</summary>

<read_first>
- pytest.ini (addopts)
- .github/workflows/test.yml (makemigrations step)
</read_first>

<action>
Run verification sequence:
1. `uv run pytest tests/ --cov=app --cov-fail-under=60` — full suite passes
2. `uv run python manage.py makemigrations --check --dry-run app` — no migration drift
3. `uv run python manage.py check` — no system check errors
4. `git status` — clean working tree (no uncommitted changes)
5. `git branch | Select-String "gsd-reviewfix"` — no stale branch
</action>

<acceptance_criteria>
- All 5 commands exit 0 (or return expected output)
- No uncommitted changes in working tree
- No stale review-fix branch
</acceptance_criteria>
</task>
