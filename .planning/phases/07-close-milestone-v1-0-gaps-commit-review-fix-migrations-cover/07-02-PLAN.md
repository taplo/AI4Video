---
phase: 07
plan: 07-02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/test_analysis_manager.py
  - tests/test_stream_api.py
  - tests/test_views_remaining.py
  - tests/test_utils_core.py
  - .gitignore
  - scripts/verify.ps1
autonomous: true
requirements:
  - phase05-D-05
  - phase05-D-08
  - phase05-D-09
  - phase05-D-10
---

# Plan 07-02: Coverage Gate 17% → 60%

## Objective

Raise project-wide test coverage from 17% baseline (12,706 statements / 2,159 covered) to ≥60% (7,624 covered = +5,465 statements needed) by writing high-value tests targeting the largest uncovered modules.

## must_haves

- `uv run pytest tests/ --cov=app --cov-fail-under=60` exits 0
- No coverage exclusions added (D-09)
- .gitignore __pycache__ pattern fixed
- scripts/verify.ps1 updated with explicit cov flags

## truths

- Baseline: 12,706 statements, 2,159 covered (17%)
- Target: 7,624 covered (+5,465 needed)
- Views carry 3,851 uncovered statements — highest ROI
- AnalysisManager (589 stmts/20%) + pipeline (729/15%) are the largest non-view targets
- GlobalUtils (175/45%), ZLMediaKitApi (252/12%), OSSystem (221/9%) are high-value utility targets
- No coverage exclusions — D-09 locked

## threats

- T-07-03: StreamView (755 stmts/4%) may require complex mocking of websocket/streaming
- T-07-04: Some view tests may need GB28181/SIP protocol mocks

## <threat_model>

- No security-relevant changes in this plan — test infrastructure only
- No new attack surface
- Coverage gate enforces quality baseline

</threat_model>

<task id="07-02-01" type="execute" priority="high">
<summary>Write tests/test_analysis_manager.py — manager + pipeline coverage</summary>

<read_first>
- app/analysis/manager.py (AnalysisManager class — 589 stmts, 20% covered)
- app/analysis/pipeline.py (pipeline functions — 729 stmts, 15% covered)
- tests/conftest.py (mock_g_zlm, mock_g_config, mock_g_logger fixtures)
- tests/test_auth.py (fixture usage patterns)
</read_first>

<action>
Create tests/test_analysis_manager.py covering:
1. AnalysisManager initialization and configuration
2. Pipeline execution paths (start, stop, error handling)
3. Mock dependencies using mock_g_zlm, mock_g_config from conftest
4. Target: +500 covered statements minimum
</action>

<acceptance_criteria>
- `uv run pytest tests/test_analysis_manager.py -v` exits 0
- File contains at least 8 test methods
- Coverage delta: `uv run pytest tests/test_analysis_manager.py --cov=app.analysis.manager --cov-report=term-missing` shows >30% for manager.py
</acceptance_criteria>
</task>

<task id="07-02-02" type="execute" priority="high">
<summary>Write tests/test_stream_api.py — StreamView coverage</summary>

<read_first>
- app/views/StreamView.py (755 stmts, 4% covered — largest single target)
- tests/conftest.py (fixtures)
- tests/test_stream.py (existing stream tests — see patterns)
- pytest.ini (addopts)
</read_first>

<action>
Create tests/test_stream_api.py covering StreamView endpoints:
1. Use Django test client with session auth (session["user"] = "admin")
2. Test GET/POST endpoints for stream management
3. Mock external dependencies (ZLMediaKit, GB28181)
4. Target: +400 covered statements minimum
</action>

<acceptance_criteria>
- `uv run pytest tests/test_stream_api.py -v` exits 0
- File contains at least 6 test methods
- Coverage delta: `uv run pytest tests/test_stream_api.py --cov=app.views.StreamView --cov-report=term-missing` shows >15% for StreamView.py
</acceptance_criteria>
</task>

<task id="07-02-03" type="execute" priority="high">
<summary>Write tests/test_views_remaining.py — multi-view coverage</summary>

<read_first>
- app/views/ControlView.py (457 stmts, 6%)
- app/views/SmallModelView.py (614 stmts, 6%)
- app/views/InnerlView.py (233 stmts, 11%)
- app/views/NvrView.py (283 stmts, 8%)
- app/views/LLMView.py (232 stmts, 7%)
- app/views/IndexView.py (105 stmts, 10%)
- app/views/SystemView.py (223 stmts, 8%)
- tests/conftest.py (fixtures)
- tests/test_api.py (Django client session auth pattern)
</read_first>

<action>
Create tests/test_views_remaining.py covering all 7 views:
1. Each view gets a test class with 2-4 test methods
2. Use Django test client with session auth pattern
3. Mock external dependencies per view (NVR, LLM, SmallModel, etc.)
4. Target: +800 covered statements minimum across all 7 views
</action>

<acceptance_criteria>
- `uv run pytest tests/test_views_remaining.py -v` exits 0
- File contains 7 test classes (one per view)
- Each class has at least 2 test methods
- Coverage improves for all 7 target view files
</acceptance_criteria>
</task>

<task id="07-02-04" type="execute" priority="high">
<summary>Write tests/test_utils_core.py — utility module coverage</summary>

<read_first>
- app/utils/GpuInfo.py (288 stmts, 0% covered)
- app/utils/ZLMediaKitApi.py (252 stmts, 12%)
- app/utils/OSSystem.py (221 stmts, 9%)
- app/utils/MediaServerManager.py (106 stmts, 0%)
- app/utils/GlobalUtils.py (175 stmts, 45%)
- tests/conftest.py (fixtures)
</read_first>

<action>
Create tests/test_utils_core.py covering 5 utility modules:
1. Test GpuInfo: GPU detection, info parsing
2. Test ZLMediaKitApi: API calls, response parsing
3. Test OSSystem: system info, process management
4. Test MediaServerManager: server lifecycle
5. Test GlobalUtils: remaining uncovered methods
6. Target: +400 covered statements minimum
</action>

<acceptance_criteria>
- `uv run pytest tests/test_utils_core.py -v` exits 0
- File contains 5 test classes (one per utility)
- Each class has at least 2 test methods
- Coverage improves for all 5 target utility files
</acceptance_criteria>
</task>

<task id="07-02-05" type="execute" priority="medium">
<summary>Fix .gitignore __pycache__ pattern</summary>

<read_first>
- .gitignore (current state — line 32-33 has `app/migrations/*` already removed in 07-01, check __pycache__ pattern)
</read_first>

<action>
Edit .gitignore: change any `__pycache__/*` patterns to `__pycache__/` (directory-level, not root-anchored). This ensures tests/__pycache__/ is properly ignored. The pattern `__pycache__/*` is root-anchored and doesn't match subdirectories.
</action>

<acceptance_criteria>
- `.gitignore` contains `__pycache__/` (not `__pycache__/*`)
- `git check-ignore tests/__pycache__/` returns 0 (ignored)
</acceptance_criteria>
</task>

<task id="07-02-06" type="execute" priority="medium">
<summary>Update scripts/verify.ps1 with explicit cov flags</summary>

<read_first>
- scripts/verify.ps1 (current state — find the pytest invocation line)
- .github/workflows/test.yml (reference: has --cov-fail-under=60 in addopts)
</read_first>

<action>
Edit scripts/verify.ps1 to add explicit `--cov=app --cov-fail-under=60` flags to the pytest invocation, ensuring gate symmetry with .github/workflows/test.yml. This is D-08 (CI enforces gate) alignment.
</action>

<acceptance_criteria>
- `scripts/verify.ps1` contains `--cov-fail-under=60`
- `scripts/verify.ps1` contains `--cov=app`
</acceptance_criteria>
</task>

<task id="07-02-07" type="execute" priority="high">
<summary>Verify Plan 07-02 — Full coverage gate</summary>

<read_first>
- pytest.ini (addopts: --cov-fail-under=60)
</read_first>

<action>
Run: `uv run pytest tests/ --cov=app --cov-fail-under=60`
</action>

<acceptance_criteria>
- Command exits 0
- Coverage report shows ≥60% for app/ package
- No warnings about coverage exclusions
</acceptance_criteria>
</task>
