---
phase: 07
plan: 07-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/middleware.py
  - app/utils/GB28181SipServer.py
  - app/models.py
  - app/migrations/0001_initial.py
  - app/migrations/0002_auditlog.py
  - app/migrations/0003_alter_*.py
  - .gitignore
  - app/fields.py
  - app/views/UserView.py
  - manage.py
  - requirements.txt
  - ai4video.sqlite3
  - tests/test_phase06_fixes.py
  - .planning/phases/04-engineering-hardening/04-REVIEW-FIX.md
  - .planning/phases/06-other-upgrades/06-REVIEW-FIX.md
autonomous: true
requirements:
  - phase06-REVIEW
  - phase07-D-01
  - phase07-D-02
  - phase07-D-03
  - phase07-D-04
---

# Plan 07-01: Commit Review-Fix

## Objective

Commit the review-fix work from phase06 (CR-01, CR-02, WR-01..09) as a series of atomic commits, write regression tests for the security fixes, and produce review-fix documentation for both phase04 and phase06 reviews.

## must_haves

- CR-01 critical fix committed (middleware.py + GB28181SipServer.py — `/inner/` Safe-header auth)
- CR-02 critical fix committed (models.py + migrations + sqlite3 — AuditLog + version-controlled migrations)
- WR-01..09 warnings committed (fields.py, UserView.py, manage.py, requirements.txt)
- .gitignore updated to un-gitignore `app/migrations/*`
- tests/test_phase06_fixes.py exists and passes
- 04-REVIEW-FIX.md exists with commit refs for 6 criticals
- 06-REVIEW-FIX.md exists with commit refs for CR/WR

## truths

- Each commit addresses exactly one concern (atomic commits)
- CR-01 fixes `/inner/` endpoint authentication bypass
- CR-02 adds AuditLog model and version-controls Django migrations
- No new features — this plan only commits existing uncommitted work
- Test file covers both CR-01 (Safe-header regression) and CR-02 (audit log)

## threats

- T-07-01: Stale worktree for gsd-reviewfix/06-17660 must be removed before branch deletion (D-03)
- T-07-02: .gitignore line 32-33 currently ignores app/migrations/* — must be fixed before C2

## <threat_model>

- ASVS L1 compliance required
- CR-01: Authentication bypass on /inner/ endpoints — Safe-header auth enforcement
- CR-02: AuditLog model for security event tracking — data integrity via migrations
- No new attack surface introduced — fixes only

</threat_model>

<task id="07-01-01" type="execute" priority="high">
<summary>C1: Commit CR-01 critical fix — Safe-header auth enforcement</summary>

<read_first>
- app/middleware.py (current state — see Safe-header logic)
- app/utils/GB28181SipServer.py (current state — see /inner/ auth)
- .planning/phases/07-close-milestone-v1-0-gaps-commit-review-fix-migrations-cover/07-CONTEXT.md (D-01 decision)
</read_first>

<action>
Stage and commit the CR-01 security fix. The changes in app/middleware.py and app/utils/GB28181SipServer.py enforce Safe-header authentication on /inner/ endpoints. Run:
1. `git add app/middleware.py app/utils/GB28181SipServer.py`
2. `git commit -m "fix(security): CR-01 — enforce /inner/ Safe-header auth in middleware + GB28181SipServer"`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows commit with message containing "CR-01"
- `git diff HEAD~1 --stat` shows only middleware.py and GB28181SipServer.py
- No other files staged in this commit
</acceptance_criteria>
</task>

<task id="07-01-02" type="execute" priority="high">
<summary>C2: Commit CR-02 critical fix — AuditLog + version-controlled migrations</summary>

<read_first>
- .gitignore (lines 32-33 currently have `app/migrations/*`)
- app/models.py (AuditLog model)
- app/migrations/ (0001_initial.py, 0002_auditlog.py, 0003_alter_*.py)
- ai4video.sqlite3 (schema with AuditLog)
</read_first>

<action>
1. Edit .gitignore: remove or comment out lines 32-33 (`app/migrations/*`) so migrations are version-controlled
2. `git add .gitignore`
3. `git add app/models.py app/migrations/ ai4video.sqlite3`
4. `git commit -m "fix(security): CR-02 — add AuditLog model + version-controlled migrations"`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows commit with message containing "CR-02"
- `git diff HEAD~1 --stat` shows .gitignore, models.py, migrations/, sqlite3
- `.gitignore` no longer contains `app/migrations/*`
- `git ls-files app/migrations/` shows 0001_initial.py, 0002_auditlog.py, 0003_alter_*.py are tracked
</acceptance_criteria>
</task>

<task id="07-01-03" type="execute" priority="high">
<summary>C3: Commit WR-01..09 warnings fix</summary>

<read_first>
- app/fields.py (current state)
- app/views/UserView.py (current state)
- manage.py (current state)
- requirements.txt (current state)
</read_first>

<action>
Stage and commit the warnings fix. Run:
1. `git add app/fields.py app/views/UserView.py manage.py requirements.txt`
2. `git commit -m "fix(warnings): close WR-01..09 — fields, UserView, manage.py, requirements"`
</action>

<acceptance_criteria>
- `git log --oneline -1` shows commit with message containing "WR-01"
- `git diff HEAD~1 --stat` shows only fields.py, UserView.py, manage.py, requirements.txt
</acceptance_criteria>
</task>

<task id="07-01-04" type="execute" priority="high">
<summary>C4: Write + commit tests/test_phase06_fixes.py</summary>

<read_first>
- tests/test_auth.py (fixture patterns: mock_g_zlm, pytest-django db usage)
- tests/conftest.py (shared fixtures: mock_g_zlm, mock_g_config, mock_g_logger)
- app/middleware.py (Safe-header auth logic to test)
- app/models.py (AuditLog model to test)
- app/utils/GB28181SipServer.py (/inner/ auth to test)
- pytest.ini (addopts: --cov=app --cov-report=term-missing)
</read_first>

<action>
Create tests/test_phase06_fixes.py with:
1. Test class `TestCR01SafeHeaderAuth` — verify /inner/ endpoints require Safe header, reject unauthorized requests, accept valid Safe header
2. Test class `TestCR02AuditLog` — verify AuditLog model creates records, verify migration applies cleanly
3. Use Django test client with session auth pattern (set session["user"] for authenticated requests)
4. Import fixtures from conftest.py (mock_g_zlm, mock_g_config)

Then commit:
1. `git add tests/test_phase06_fixes.py`
2. `git commit -m "test(security): add regression tests for CR-01 and CR-02"`
</action>

<acceptance_criteria>
- `uv run pytest tests/test_phase06_fixes.py -v` exits 0
- File contains class TestCR01SafeHeaderAuth with at least 2 test methods
- File contains class TestCR02AuditLog with at least 2 test methods
- `git log --oneline -1` shows commit with message containing "CR-01" and "CR-02"
</acceptance_criteria>
</task>

<task id="07-01-05" type="execute" priority="medium">
<summary>C5: Write + commit 04-REVIEW-FIX.md and 06-REVIEW-FIX.md</summary>

<read_first>
- .planning/phases/02-onnx-fix/02-REVIEW-FIX.md (format precedent: frontmatter + per-finding blocks with Files modified / Applied fix / Change details)
- .planning/v1.0-MILESTONE-AUDIT.md (phase04-REVIEW and phase06-REVIEW findings)
- .planning/phases/07-close-milestone-v1-0-gaps-commit-review-fix-migrations-cover/07-CONTEXT.md (D-04 decision)
</read_first>

<action>
1. Create `.planning/phases/04-engineering-hardening/04-REVIEW-FIX.md` following 02-REVIEW-FIX.md format:
   - Frontmatter: phase 04, fixed: 6, skipped: 0
   - Per-finding block for each of the 6 criticals with Files modified, Applied fix, Change details, Commit ref
   - Commit refs point to C1/C2/C3 commits above

2. Create `.planning/phases/06-other-upgrades/06-REVIEW-FIX.md`:
   - Frontmatter: phase 06, fixed: N (CR-01 + CR-02 + WR count), skipped: 0
   - Per-finding block for CR-01, CR-02, WR-01..09 with commit refs

3. `git add .planning/phases/04-engineering-hardening/04-REVIEW-FIX.md .planning/phases/06-other-upgrades/06-REVIEW-FIX.md`
4. `git commit -m "docs: add REVIEW-FIX.md for phase04 and phase06 with commit refs"`
</action>

<acceptance_criteria>
- `.planning/phases/04-engineering-hardening/04-REVIEW-FIX.md` exists
- `.planning/phases/06-other-upgrades/06-REVIEW-FIX.md` exists
- Both files contain `**Commit ref:**` fields pointing to actual git commit hashes
- `git log --oneline -1` shows commit with message containing "REVIEW-FIX"
</acceptance_criteria>
</task>

<task id="07-01-06" type="execute" priority="high">
<summary>Verify Plan 07-01</summary>

<read_first>
- pytest.ini (addopts)
</read_first>

<action>
Run: `uv run pytest tests/test_phase06_fixes.py -v`
</action>

<acceptance_criteria>
- Command exits 0
- All test methods in TestCR01SafeHeaderAuth pass
- All test methods in TestCR02AuditLog pass
</acceptance_criteria>
</task>
