---
phase: 05
plan_id: "05-04"
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - tests/test_auth.py
  - tests/test_stream.py
autonomous: true
requirements: []
---

# Plan 05-04: Auth and Stream Integration Tests

## Objective
Create integration tests for authentication and stream management.

## Tasks

### Task 1: Create tests/test_auth.py
<read_first>
- app/views/IndexView.py (login logic)
- app/middleware.py (auth flow)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (auth test patterns)
</read_first>

<action>
Create `tests/test_auth.py` with `@pytest.mark.django_db` decorator:
- `TestLogin`: test login page renders, valid credentials login, invalid credentials rejected, session created on login, logout clears session
- `TestCaptcha`: test captcha generation, captcha disabled when config off
- `TestBruteForceProtection`: test rate limit after n failures
</action>

<acceptance_criteria>
- File `tests/test_auth.py` exists
- All test functions use `@pytest.mark.django_db` decorator
- Contains at least 8 test functions
- Running `pytest tests/test_auth.py -v` passes all tests
</acceptance_criteria>

### Task 2: Create tests/test_stream.py
<read_first>
- app/views/StreamView.py (stream CRUD operations)
- app/models.py (StreamModel)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (stream test patterns)
</read_first>

<action>
Create `tests/test_stream.py` with `@pytest.mark.django_db` decorator and `mock_g_zlm` fixture:
- `TestStreamCRUD`: test create stream, update stream, delete stream, list streams, get stream by id
- `TestStreamProxy`: test add stream proxy, delete stream proxy
</action>

<acceptance_criteria>
- File `tests/test_stream.py` exists
- All test functions use `@pytest.mark.django_db` decorator
- Contains at least 7 test functions
- Running `pytest tests/test_stream.py -v` passes all tests
</acceptance_criteria>

## Verification
- Run `pytest tests/test_auth.py tests/test_stream.py -v`
