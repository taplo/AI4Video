---
phase: 05
plan_id: "05-05"
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - tests/test_algorithm.py
  - tests/test_onnx_engine.py
autonomous: true
requirements: []
---

# Plan 05-05: Algorithm and ONNX Engine Tests

## Objective
Create tests for algorithm management and ONNX engine (mocked).

## Tasks

### Task 1: Create tests/test_algorithm.py
<read_first>
- app/views/AlgorithmView.py (algorithm CRUD)
- app/analysis/engines/factory.py (engine factory)
- app/models.py (AlgorithmModel)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (algorithm test patterns)
</read_first>

<action>
Create `tests/test_algorithm.py` with `@pytest.mark.django_db` decorator:
- `TestAlgorithmCRUD`: test create, update, delete, list algorithms
- `TestEngineFactory`: test create onnx engine, create yolo engine, unknown engine raises, unavailable engine raises, list engines, device options
</action>

<acceptance_criteria>
- File `tests/test_algorithm.py` exists
- All test functions use `@pytest.mark.django_db` decorator where needed
- Contains at least 10 test functions
- Running `pytest tests/test_algorithm.py -v` passes all tests
</acceptance_criteria>

### Task 2: Create tests/test_onnx_engine.py
<read_first>
- app/analysis/engines/onnx_engine.py (OnnxEngine class)
- app/analysis/engines/base.py (BaseEngine interface)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (ONNX mock patterns)
</read_first>

<action>
Create `tests/test_onnx_engine.py` with mocked ONNX runtime:
- `TestOnnxEngineUnit`: test is_available when deps installed/missing, version returns string, providers for cpu/cuda
- `TestOnnxEngineLoad`: test load missing model returns false, load sets loaded flag, load with mock session
- `TestOnnxEngineDetect`: test detect before load returns empty, detect with mock session, detect handles exception

Use `@pytest.fixture` with `unittest.mock.patch` to mock `onnxruntime.InferenceSession`.
</action>

<acceptance_criteria>
- File `tests/test_onnx_engine.py` exists
- Uses `unittest.mock.patch` to mock onnxruntime
- Contains at least 9 test functions
- Running `pytest tests/test_onnx_engine.py -v` passes all tests
</acceptance_criteria>

## Verification
- Run `pytest tests/test_algorithm.py tests/test_onnx_engine.py -v`
