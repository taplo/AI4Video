---
phase: 05
plan_id: "05-06"
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - tests/test_analysis_pipeline.py
  - tests/test_api.py
  - .github/workflows/test.yml
autonomous: true
requirements: []
---

# Plan 05-06: Pipeline, API Tests, and CI Configuration

## Objective
Create pipeline lifecycle tests, API integration tests, and GitHub Actions CI workflow.

## Tasks

### Task 1: Create tests/test_analysis_pipeline.py
<read_first>
- app/analysis/pipeline.py (CameraPipeline class)
- app/analysis/manager.py (AnalysisManager)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (pipeline test patterns)
</read_first>

<action>
Create `tests/test_analysis_pipeline.py` with mocked dependencies:
- `TestPipelineLifecycle`: test pipeline initialization, pipeline start/stop, pipeline frame processing (mocked capture)

Use fixtures to mock ZLMediaKit, ONNX engine, and capture device.
</action>

<acceptance_criteria>
- File `tests/test_analysis_pipeline.py` exists
- Uses mocks for external dependencies
- Contains at least 3 test functions
- Running `pytest tests/test_analysis_pipeline.py -v` passes all tests
</acceptance_criteria>

### Task 2: Create tests/test_api.py
<read_first>
- app/urls.py (URL routing)
- app/views/HealthView.py (health endpoint)
- app/views/StreamView.py (stream API)
- app/views/AlgorithmView.py (algorithm API)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (API test patterns)
</read_first>

<action>
Create `tests/test_api.py` with `@pytest.mark.django_db` decorator and Django test client:
- `TestHealthEndpoint`: test health returns 200, health returns JSON
- `TestStreamAPI`: test openIndex requires auth, api_openAdd with valid params, api_openAdd without auth
- `TestAlgorithmAPI`: test list algorithms, add algorithm
</action>

<acceptance_criteria>
- File `tests/test_api.py` exists
- Uses Django test client for HTTP requests
- Contains at least 7 test functions
- Running `pytest tests/test_api.py -v` passes all tests
</acceptance_criteria>

### Task 3: Create .github/workflows/test.yml
<read_first>
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (CI configuration)
</read_first>

<action>
Create `.github/workflows/test.yml` with:
- Trigger on push/PR to master
- Python matrix: 3.11, 3.12
- Steps: checkout, setup-python, cache pip, install deps (requirements.txt + requirements-dev.txt), flake8 lint, pytest with coverage
- Environment variables: DJANGO_SECRET_KEY, DEBUG
- Upload coverage artifact on Python 3.11
</action>

<acceptance_criteria>
- File `.github/workflows/test.yml` exists
- Contains valid YAML syntax
- Triggers on push and pull_request to master
- Includes Python 3.11 and 3.12 matrix
- Includes flake8 lint step
- Includes pytest with coverage step
</acceptance_criteria>

## Verification
- Run `pytest tests/test_analysis_pipeline.py tests/test_api.py -v`
- Validate YAML syntax of `.github/workflows/test.yml`
