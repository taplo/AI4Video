---
phase: 05
plan_id: "05-03"
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_models.py
  - tests/test_middleware.py
autonomous: true
requirements: []
---

# Plan 05-03: Model and Middleware Tests

## Objective
Create tests for Django models and authentication middleware.

## Tasks

### Task 1: Create tests/test_models.py
<read_first>
- app/models.py (source of truth for all model definitions)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (model test patterns)
</read_first>

<action>
Create `tests/test_models.py` with `@pytest.mark.django_db` decorator on all tests:
- `TestStreamModel`: test create, str returns nickname, default forward_state, cascade delete
- `TestAlgorithmModel`: test create, default algorithm_type, engine choices valid
- `TestBizAlgorithmModel`: test create, flow_type choices, post_process choices
- `TestZoneModel`: test create, many_to_many algorithms
- `TestAlarmModel`: test create, indexes exist
- `TestRecordingModel`: test create
- `TestLLMModel`: test create
- `TestLogModel`: test create
</action>

<acceptance_criteria>
- File `tests/test_models.py` exists
- All test functions use `@pytest.mark.django_db` decorator
- Contains at least 15 test functions
- Running `pytest tests/test_models.py -v` passes all tests
</acceptance_criteria>

### Task 2: Create tests/test_middleware.py
<read_first>
- app/middleware.py (source of truth for SimpleMiddleware)
- app/urls.py (whitelist paths)
- .planning/phases/05-test-infrastructure/05-RESEARCH.md (middleware test patterns)
</read_first>

<action>
Create `tests/test_middleware.py` with `@pytest.mark.django_db` decorator:
- `TestSimpleMiddleware`: test whitelist paths bypass auth (/login, /static/, /api/health), authenticated user passes, unauthenticated redirects to login, logged_in user at login redirects home, open API with valid/invalid safe header
</action>

<acceptance_criteria>
- File `tests/test_middleware.py` exists
- All test functions use `@pytest.mark.django_db` decorator
- Contains at least 7 test functions
- Running `pytest tests/test_middleware.py -v` passes all tests
</acceptance_criteria>

## Verification
- Run `pytest tests/test_models.py tests/test_middleware.py -v`
