---
phase: 04-engineering-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/settings.py
  - app/middleware.py
  - app/views/LLMView.py
  - app/utils/GlobalUtils.py
  - app/views/StorageView.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "DEBUG mode is controlled by environment variable, defaults to False"
    - "ALLOWED_HOSTS is configurable via environment variable"
    - "X_FRAME_OPTIONS is set to SAMEORIGIN"
    - "Auth bypass via '/open' in path is eliminated"
    - "@csrf_exempt is removed from LLMView.api_openTest"
    - "External telemetry code is completely removed"
    - "Path traversal via crafted filenames is prevented"
  artifacts:
    - path: "framework/settings.py"
      provides: "Security settings via environment variables"
      contains: "os.environ.get('DEBUG'"
    - path: "app/middleware.py"
      provides: "Auth bypass fix"
      contains: "path.startswith('/open')"
    - path: "app/views/LLMView.py"
      provides: "CSRF protection enabled"
      does_not_contain: "@csrf_exempt"
    - path: "app/views/StorageView.py"
      provides: "Path traversal protection"
      contains: "os.path.basename(filename)"
  key_links:
    - from: "framework/settings.py"
      to: "environment variables"
      via: "os.environ.get"
      pattern: "os\\.environ\\.get\\("
    - from: "app/middleware.py"
      to: "request.path_info"
      via: "startswith check"
      pattern: "path\\.startswith\\('/open'\\)"
---

<objective>
Fix all 8 known security vulnerabilities in one pass (D-01).

Purpose: Establish a security baseline before adding new features. These are critical vulnerabilities that must be fixed before any other work.
Output: Updated settings.py, middleware.py, LLMView.py, GlobalUtils.py, StorageView.py with all security fixes applied.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-hardening/04-CONTEXT.md
@.planning/phases/04-engineering-hardening/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From framework/settings.py:
```python
import os
from dotenv import load_dotenv
load_dotenv(BASE_DIR / '.env')
# Current: DEBUG = True, ALLOWED_HOSTS = ["*"], X_FRAME_OPTIONS = 'ALLOWALL'
```

From app/middleware.py:
```python
class SimpleMiddleware(MiddlewareMixin):
    def process_request(self, request):
        path = request.path_info
        # Current bug: if '/open' in path: (line 38)
```

From app/views/LLMView.py:
```python
from django.views.decorators.csrf import csrf_exempt
# @csrf_exempt decorator on api_openTest (line 258)
```

From app/views/StorageView.py:
```python
filename = params.get("filename", "").strip()
# Current: no basename validation, direct os.path.join with user input
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix settings.py security configuration (D-02, D-03, D-07)</name>
  <files>framework/settings.py</files>
  <read_first>
    - framework/settings.py (current state: DEBUG=True, ALLOWED_HOSTS=["*"], X_FRAME_OPTIONS='ALLOWALL')
    - .env (check if it exists, what variables are already set)
  </read_first>
  <action>
Per D-02: Change line 36 from `DEBUG = True` to `DEBUG = os.environ.get('DEBUG', 'False').lower() == 'true'`. The comment on line 35 should be updated to explain the environment variable control.

Per D-03: Change line 38 from `ALLOWED_HOSTS = ["*"]` to `ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', 'localhost').split(',')`. Add a comment explaining the environment variable.

Per D-07: Change line 148 from `X_FRAME_OPTIONS = 'ALLOWALL'` to `X_FRAME_OPTIONS = 'SAMEORIGIN'`. Remove the commented-out line 149 that shows the SAMEORIGIN alternative.

Also add these security best practices from RESEARCH.md Pattern 1:
- After the X_FRAME_OPTIONS line, add: `CSRF_COOKIE_SECURE = not DEBUG`
- After that, add: `CSRF_COOKIE_HTTPONLY = True`

The `os` module is already imported on line 14, and `load_dotenv` is already called on line 21, so no new imports needed.
  </action>
  <verify>
    <automated>python -c "import re; content=open('framework/settings.py').read(); assert 'os.environ.get' in content, 'Missing env var for DEBUG'; assert \"'False'\" in content, 'Missing default False'; assert \"'localhost'\" in content, 'Missing default localhost'; assert \"'SAMEORIGIN'\" in content, 'Missing SAMEORIGIN'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - framework/settings.py line 36 contains: `DEBUG = os.environ.get('DEBUG', 'False').lower() == 'true'`
    - framework/settings.py line 38 contains: `ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', 'localhost').split(',')`
    - framework/settings.py contains: `X_FRAME_OPTIONS = 'SAMEORIGIN'`
    - framework/settings.py contains: `CSRF_COOKIE_SECURE = not DEBUG`
    - framework/settings.py contains: `CSRF_COOKIE_HTTPONLY = True`
    - No `DEBUG = True` or `ALLOWED_HOSTS = ["*"]` or `X_FRAME_OPTIONS = 'ALLOWALL'` remains
  </acceptance_criteria>
  <done>DEBUG defaults to False via env var, ALLOWED_HOSTS defaults to localhost via env var, X_FRAME_OPTIONS set to SAMEORIGIN, CSRF cookies secured</done>
</task>

<task type="auto">
  <name>Task 2: Fix middleware, LLMView, GlobalUtils, StorageView security issues (D-04, D-05, D-06, D-08)</name>
  <files>app/middleware.py, app/views/LLMView.py, app/utils/GlobalUtils.py, app/views/StorageView.py</files>
  <read_first>
    - app/middleware.py (full file, 52 lines - auth bypass on line 38)
    - app/views/LLMView.py (lines 1-10 for imports, line 258 for @csrf_exempt)
    - app/utils/GlobalUtils.py (full file, 289 lines - check for any remaining telemetry code)
    - app/views/StorageView.py (full file, 83 lines - path traversal on line 36-49)
  </read_first>
  <action>
Per D-06 (middleware.py): On line 38, change `if '/open' in path:` to `if path.startswith('/open'):`. This prevents paths like `/notopen/` from bypassing authentication. The existing AUTH_WHITELIST_PREFIXES on lines 9-16 already handle specific whitelisted paths, so this fix only affects the Safe header authentication path.

Per D-05 (LLMView.py): Remove the `@csrf_exempt` decorator on line 258. Also remove the `from django.views.decorators.csrf import csrf_exempt` import on line 4 (if it becomes unused). Note: The frontend must send CSRF tokens for this endpoint to work - but that's a frontend concern, not this task.

Per D-04 (GlobalUtils.py): Search for and remove any remaining external telemetry code. The CONCERNS.md mentions lines 307, 369 with yuturuishi.com references, but the file is now 289 lines. Verify the file has no external HTTP calls to yuturuishi.com or similar domains. If any telemetry code exists (like CheckServerUtils class or reportHeart calls), remove it completely.

Per D-08 (StorageView.py): After line 36 (`filename = params.get("filename", "").strip()`), add basename validation:
```python
filename = os.path.basename(filename)
if not filename or filename != params.get("filename", "").strip():
    return f_responseJson({"code": 0, "msg": "Invalid filename"})
```
This prevents path traversal attacks like `../../etc/passwd` by stripping directory components.
  </action>
  <verify>
    <automated>python -c "import re; mw=open('app/middleware.py').read(); assert \"path.startswith('/open')\" in mw, 'Auth bypass not fixed'; assert \"'/open' in path\" not in mw, 'Old auth bypass still present'; llm=open('app/views/LLMView.py').read(); assert '@csrf_exempt' not in llm, 'csrf_exempt still present'; sv=open('app/views/StorageView.py').read(); assert 'os.path.basename(filename)' in sv, 'Path traversal not fixed'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - app/middleware.py contains `path.startswith('/open')` and does NOT contain `'/open' in path`
    - app/views/LLMView.py does NOT contain `@csrf_exempt` decorator on any view function
    - app/views/LLMView.py does NOT import csrf_exempt (if it was only used for that one decorator)
    - app/views/StorageView.py contains `os.path.basename(filename)` before the filepath join
    - app/views/StorageView.py returns error response if filename changes after basename
    - app/utils/GlobalUtils.py contains no references to yuturuishi.com or external telemetry
  </acceptance_criteria>
  <done>Auth bypass fixed, CSRF protection enabled on LLMView, external telemetry removed, path traversal prevented via basename validation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→middleware | Untrusted request paths cross here for auth check |
| client→LLMView | Untrusted POST data crosses here, CSRF token required |
| client→StorageView | Untrusted filename parameter crosses here for file download |
| settings→runtime | Environment variables cross here for security configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | middleware.py auth bypass | mitigate | D-06: path.startswith('/open') prevents substring match bypass |
| T-04-02 | Tampering | LLMView CSRF | mitigate | D-05: Remove @csrf_exempt, require CSRF tokens |
| T-04-03 | Information Disclosure | settings.py DEBUG mode | mitigate | D-02: Environment variable control, defaults to False |
| T-04-04 | Tampering | StorageView path traversal | mitigate | D-08: os.path.basename() strips directory components |
| T-04-05 | Information Disclosure | GlobalUtils telemetry | mitigate | D-04: Remove all external data transmission code |
| T-04-06 | Tampering | Clickjacking via iframe | mitigate | D-07: X_FRAME_OPTIONS = 'SAMEORIGIN' |
| T-04-07 | Information Disclosure | ALLOWED_HOSTS wildcard | mitigate | D-03: Environment variable with localhost default |
</threat_model>

<verification>
1. grep -n "DEBUG = True" framework/settings.py should return empty
2. grep -n "ALLOWED_HOSTS = \[\"\\*\"\]" framework/settings.py should return empty
3. grep -n "X_FRAME_OPTIONS = 'ALLOWALL'" framework/settings.py should return empty
4. grep -n "'/open' in path" app/middleware.py should return empty
5. grep -n "@csrf_exempt" app/views/LLMView.py should return empty
6. grep -n "yuturuishi" app/utils/GlobalUtils.py should return empty
7. grep -n "os.path.basename(filename)" app/views/StorageView.py should return non-empty
</verification>

<success_criteria>
All 8 security vulnerabilities fixed:
- D-02: DEBUG via env var (defaults False)
- D-03: ALLOWED_HOSTS via env var (defaults localhost)
- D-04: External telemetry removed
- D-05: @csrf_exempt removed from LLMView
- D-06: Auth bypass fixed (startswith instead of 'in')
- D-07: Clickjacking fixed (SAMEORIGIN)
- D-08: Path traversal fixed (basename validation)
</success_criteria>

<output>
Create `.planning/phases/04-engineering-hardening/04-01-SUMMARY.md` when done
</output>
