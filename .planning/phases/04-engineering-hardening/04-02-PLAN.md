---
phase: 04-engineering-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/views/ViewsBase.py
  - app/utils/Database.py
  - app/analysis/pipeline.py
  - app/analysis/manager.py
autonomous: true
requirements: [D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "All API errors return unified JSON format {code, msg, detail, timestamp}"
    - "Database connection failures retry 3 times with exponential backoff"
    - "OOM condition triggers worker restart and garbage collection"
  artifacts:
    - path: "app/views/ViewsBase.py"
      provides: "Unified error response helper and error code mapping"
      contains: "f_error_response"
    - path: "app/utils/Database.py"
      provides: "DB connection retry with exponential backoff"
      contains: "retry_db_operation"
    - path: "app/analysis/manager.py"
      provides: "Memory monitoring and OOM handling"
      contains: "_memory_monitor_loop"
  key_links:
    - from: "app/views/ViewsBase.py"
      to: "all view functions"
      via: "f_error_response import"
      pattern: "from app.views.ViewsBase import"
    - from: "app/utils/Database.py"
      to: "django.db.connection"
      via: "retry wrapper"
      pattern: "retry_db_operation"
    - from: "app/analysis/manager.py"
      to: "psutil.Process"
      via: "memory monitoring"
      pattern: "psutil\\.Process"
---

<objective>
Add structured error handling with unified JSON format and resilience mechanisms (DB retry, OOM protection).

Purpose: Provide consistent error responses across all endpoints and protect against common failure modes (DB connection loss, memory exhaustion).
Output: Updated ViewsBase.py with error helpers, Database.py with retry logic, manager.py with memory monitoring.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-hardening/04-CONTEXT.md
@.planning/phases/04-engineering-hardening/04-RESEARCH.md
@.planning/phases/04-engineering-hardening/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/views/ViewsBase.py:
```python
def f_responseJson(res):
    """Current: returns HttpResponse with JSON content"""
    # ...
    return HttpResponse(json.dumps(res, default=json_dumps_default), content_type="application/json")

def f_checkRequestSafe(request):
    """Returns (ret, msg) tuple"""
    # ...
    return ret, msg
```

From app/utils/Database.py:
```python
class Database(object):
    def __init__(self, logger):
        self.logger = logger

    def select(self, sql):
        # Raw SQL query, no retry logic
        # ...

    def execute(self, sql):
        # Raw SQL execute, no retry logic
        # ...
```

From app/analysis/manager.py:
```python
class AnalysisManager(object):
    _instance = None
    _instance_lock = threading.Lock()

    def __init__(self):
        # ...
        self._shutdown_event = threading.Event()
        self._health_thread = threading.Thread(target=self._health_check_loop, name="health-check", daemon=True)
        self._health_thread.start()

    def _health_check_loop(self):
        while not self._shutdown_event.is_set():
            # Only checks pipeline futures, no memory monitoring
            self._shutdown_event.wait(timeout=30)
```

From app/analysis/pipeline.py:
```python
class CameraPipeline(object):
    def __init__(self, stream_id, stream_code, rtsp_url, ...):
        # No memory limits or OOM protection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unified error response format and error codes (D-09, D-10)</name>
  <files>app/views/ViewsBase.py</files>
  <read_first>
    - app/views/ViewsBase.py (full file, 133 lines - existing f_responseJson on line 122)
    - framework/settings.py (check DEBUG setting from Plan 01)
  </read_first>
  <action>
Per D-09 and D-10, add to ViewsBase.py after the existing f_responseJson function (after line 129):

1. Add `import time` at the top of the file (after existing imports).

2. Add a new function `f_error_response(code, msg, detail=None, status_code=400)` that returns a JsonResponse with the unified error format:
   - `code`: Error code (HTTP status * 1000 + business code, e.g., 4001001)
   - `msg`: Human-readable error message
   - `detail`: Optional additional details (string or dict)
   - `timestamp`: Current Unix timestamp as integer
   - The function should use `JsonResponse` from django.http with `status=status_code`

3. Add an `ERROR_CODES` dictionary mapping error names to their codes:
   - "db_connection_failed": 5031001
   - "db_operation_failed": 5001002
   - "auth_required": 4011001
   - "permission_denied": 4031001
   - "not_found": 4041001
   - "invalid_params": 4001001
   - "oom_detected": 5031002

4. Add a helper function `f_success_response(data=None, msg="ok")` that returns a JsonResponse with format `{code: 1000, msg: msg, data: data, timestamp: int(time.time())}`.

These functions will be used by all view functions to return consistent responses. The existing f_responseJson function should remain for backward compatibility.
  </action>
  <verify>
    <automated>python -c "content=open('app/views/ViewsBase.py').read(); assert 'import time' in content, 'Missing time import'; assert 'def f_error_response' in content, 'Missing f_error_response'; assert 'def f_success_response' in content, 'Missing f_success_response'; assert 'ERROR_CODES' in content, 'Missing ERROR_CODES'; assert '5031001' in content, 'Missing db_connection_failed code'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - app/views/ViewsBase.py contains `import time`
    - app/views/ViewsBase.py contains `def f_error_response(code, msg, detail=None, status_code=400):`
    - app/views/ViewsBase.py contains `def f_success_response(data=None, msg="ok"):`
    - app/views/ViewsBase.py contains `ERROR_CODES = {` dictionary with at least 7 entries
    - f_error_response returns JsonResponse with {code, msg, detail, timestamp}
    - f_success_response returns JsonResponse with {code: 1000, msg, data, timestamp}
    - The existing f_responseJson function remains unchanged
  </acceptance_criteria>
  <done>Unified error response format available for all views, error code mapping defined, success response helper added</done>
</task>

<task type="auto">
  <name>Task 2: Add DB connection retry with exponential backoff (D-11)</name>
  <files>app/utils/Database.py</files>
  <read_first>
    - app/utils/Database.py (full file, 32 lines - current Database class with select/execute methods)
    - app/views/ViewsBase.py (to understand existing patterns and imports)
  </read_first>
  <action>
Per D-11, modify Database.py to add retry logic with exponential backoff:

1. Add imports at the top: `import time` and `import logging` (if not already present).

2. Add a helper function `retry_db_operation(func, max_retries=3, base_delay=1.0)` outside the class:
   - Takes a callable `func` to execute
   - Retries up to `max_retries` times on exception
   - Uses exponential backoff: delay = base_delay * (2 ** attempt)
   - Logs each retry attempt with the exception and delay
   - After max_retries, raises the last exception
   - Delays: 1s, 2s, 4s for 3 retries

3. Modify the `select` method:
   - Wrap the database operation in a nested function `_execute()`
   - Call `retry_db_operation(_execute)` instead of direct execution
   - The existing error handling (returning empty list on failure) should remain after retries are exhausted

4. Modify the `execute` method:
   - Wrap the database operation in a nested function `_execute()`
   - Call `retry_db_operation(_execute)` instead of direct execution
   - The existing error handling (returning False on failure) should remain after retries are exhausted

5. Use `self.logger` for logging retry attempts (the logger is already passed in __init__).

Do NOT use the tenacity library - implement retry manually per the research recommendation, as it's simpler and has no external dependency.
  </action>
  <verify>
    <automated>python -c "content=open('app/utils/Database.py').read(); assert 'def retry_db_operation' in content, 'Missing retry function'; assert 'max_retries' in content, 'Missing max_retries param'; assert 'base_delay' in content, 'Missing base_delay param'; assert '2 ** attempt' in content or '2**attempt' in content, 'Missing exponential backoff'; assert 'def select' in content, 'Missing select method'; assert 'def execute' in content, 'Missing execute method'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - app/utils/Database.py contains `def retry_db_operation(func, max_retries=3, base_delay=1.0):`
    - retry_db_operation implements exponential backoff (delay = base_delay * (2 ** attempt))
    - retry_db_operation logs retry attempts via logger.warning
    - Database.select() wraps operation in retry_db_operation
    - Database.execute() wraps operation in retry_db_operation
    - After max_retries exhausted, the original exception is raised
    - Existing error handling patterns (return empty list / False) are preserved
  </acceptance_criteria>
  <done>Database operations retry 3 times with exponential backoff (1s, 2s, 4s), logging each attempt</done>
</task>

<task type="auto">
  <name>Task 3: Add OOM protection with memory monitoring (D-12)</name>
  <files>app/analysis/manager.py</files>
  <read_first>
    - app/analysis/manager.py (full file, 735 lines - AnalysisManager class, _health_check_loop on line 721)
    - app/analysis/pipeline.py (lines 36-50 for CameraPipeline class structure)
  </read_first>
  <action>
Per D-12, add memory monitoring and OOM protection to AnalysisManager:

1. Add imports at the top of manager.py: `import psutil` and `import os` (if not already present).

2. In AnalysisManager.__init__ (around line 102), add after `self._shutdown_event = threading.Event()`:
   - `self._max_memory_mb = 2048` (2GB limit)
   - `self._memory_check_interval = 30` (seconds, per D-14)
   - Start a new daemon thread `self._memory_thread` targeting `self._memory_monitor_loop`, named "memory-monitor"

3. Add a new method `_memory_monitor_loop(self)`:
   - Loop while not `self._shutdown_event.is_set()`
   - Use `psutil.Process(os.getpid())` to get current process memory
   - Check `process.memory_info().rss / (1024 * 1024)` against `self._max_memory_mb`
   - If exceeded, call `self._handle_oom()`
   - After memory check, call `self._check_worker_health()`
   - Wait `self._memory_check_interval` seconds using `self._shutdown_event.wait()`

4. Add a new method `_handle_oom(self)`:
   - Log warning: "OOM detected, restarting workers..."
   - Acquire `self._lock`
   - Iterate `self._pipelines`, call `pipe.stop()` on each pipeline
   - Clear `self._pipelines`
   - Release lock
   - Force garbage collection: `import gc; gc.collect()`
   - Call `self.restart_inference_pool()` (existing method on line 232)

5. Add a new method `_check_worker_health(self)`:
   - This extends the existing `_health_check_loop` logic
   - Acquire `self._lock`
   - Iterate `self._pipelines`, check if future is done (existing logic from line 726-732)
   - If a pipeline failed, log the exception and restart it using existing restart pattern
   - This can be called from `_memory_monitor_loop` instead of duplicating the health check thread

Note: The existing `_health_check_loop` method (line 721) can remain as-is for backward compatibility, or be refactored to call `_check_worker_health`. The memory monitor thread runs in parallel.
  </action>
  <verify>
    <automated>python -c "content=open('app/analysis/manager.py').read(); assert 'import psutil' in content, 'Missing psutil import'; assert '_memory_monitor_loop' in content, 'Missing memory monitor'; assert '_handle_oom' in content, 'Missing OOM handler'; assert '_max_memory_mb' in content, 'Missing memory limit'; assert 'gc.collect' in content, 'Missing garbage collection'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - app/analysis/manager.py imports psutil and os
    - AnalysisManager.__init__ starts a _memory_thread daemon thread
    - _memory_monitor_loop checks process RSS memory against _max_memory_mb (2048 MB)
    - _handle_oom stops all pipelines, clears _pipelines, runs gc.collect(), restarts inference pool
    - _check_worker_health detects failed futures and logs exceptions
    - Memory check interval is 30 seconds
    - Existing _health_check_loop remains functional
  </acceptance_criteria>
  <done>OOM protection active: memory monitored every 30s, workers restarted on exceeding 2GB, garbage collection forced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view→error_response | Error format crosses from view logic to client response |
| db→retry_logic | Database failures cross from connection layer to retry handler |
| process→memory_monitor | System memory state crosses from OS to monitoring thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | Error responses | mitigate | D-09: Unified format prevents stack trace leakage |
| T-04-09 | Denial of Service | DB connection exhaustion | mitigate | D-11: Retry with backoff prevents thundering herd |
| T-04-10 | Denial of Service | Memory exhaustion | mitigate | D-12: Memory monitoring + OOM restart prevents crash |
| T-04-11 | Tampering | Error code manipulation | accept | Error codes are informational, not security-critical |
</threat_model>

<verification>
1. grep -n "def f_error_response" app/views/ViewsBase.py should return non-empty
2. grep -n "ERROR_CODES" app/views/ViewsBase.py should return non-empty
3. grep -n "def retry_db_operation" app/utils/Database.py should return non-empty
4. grep -n "import psutil" app/analysis/manager.py should return non-empty
5. grep -n "_memory_monitor_loop" app/analysis/manager.py should return non-empty
6. grep -n "_handle_oom" app/analysis/manager.py should return non-empty
</verification>

<success_criteria>
- All API errors return {code, msg, detail, timestamp} format
- DB operations retry 3 times with 1s, 2s, 4s delays
- Memory monitored every 30s, OOM triggers worker restart + GC
</success_criteria>

<output>
Create `.planning/phases/04-engineering-hardening/04-02-SUMMARY.md` when done
</output>
